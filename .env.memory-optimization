# SecretSnipe Memory Optimization Settings
# Source this file or copy to .env for Docker Compose

# === LOW MEMORY MODE (< 2GB RAM) ===
# Uncomment these for memory-constrained environments
# export SCANNER_MEMORY_LIMIT_MB=256
# export TRUFFLEHOG_CONCURRENCY=1
# export TRUFFLEHOG_MAX_DEPTH=3
# export GITLEAKS_MAX_FILE_SIZE_MB=5
# export MAX_FINDINGS_PER_SCAN=5000

# === BALANCED MODE (4-8GB RAM) - DEFAULT ===
# These are the recommended settings for most systems
export SCANNER_MEMORY_LIMIT_MB=512
export TRUFFLEHOG_CONCURRENCY=2
export TRUFFLEHOG_MAX_DEPTH=5
export GITLEAKS_MAX_FILE_SIZE_MB=10
export GITLEAKS_MAX_DEPTH=10
export MAX_FINDINGS_PER_SCAN=10000

# === HIGH PERFORMANCE MODE (16GB+ RAM) ===
# Uncomment these for systems with plenty of memory
# export SCANNER_MEMORY_LIMIT_MB=2048
# export TRUFFLEHOG_CONCURRENCY=4
# export TRUFFLEHOG_MAX_DEPTH=10
# export GITLEAKS_MAX_FILE_SIZE_MB=50
# export MAX_FINDINGS_PER_SCAN=50000

# === ADVANCED SETTINGS ===
# Scanner timeout (0 = unlimited)
export SCANNER_TIMEOUT_SECONDS=300

# Worker threads for custom scanner
export WORKER_PROCESSES=4

# Disable TruffleHog timeout (use with caution)
# export TRUFFLEHOG_NO_TIMEOUT=false

# Unlimited mode for very large scans (use with caution)
# export UNLIMITED_MODE=false
