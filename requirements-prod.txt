# Core dependencies
psycopg2-binary>=2.9.0  # PostgreSQL adapter
redis>=4.5.0  # Redis client
sqlalchemy>=2.0.0  # ORM for complex queries

# Dashboard dependencies
dash>=2.0.0
pandas>=2.0.0
plotly>=5.0.0
networkx>=3.0
scipy>=1.10.0

# Authentication
bcrypt>=4.0.0  # For password hashing

# Scanner dependencies
PyMuPDF>=1.20.0
openpyxl>=3.0.0
xlrd>=2.0.0
python-docx>=0.8.0
pillow>=10.0.0
tqdm>=4.64.0
psutil>=5.9.0
easyocr>=1.7.0  # For OCR (torch included)
pytesseract>=0.3.10  # Low-memory OCR alternative (requires tesseract-ocr binary)
xxhash>=3.2.0  # For hashing
joblib>=1.3.0  # For parallelism
numpy>=1.24.0  # For image processing with EasyOCR
watchdog>=3.0.0  # For file monitoring

# Optional regex optimization (falls back to standard regex if not available)
hyperscan>=0.2.0  # High-performance regex matching

# Scheduling and monitoring
schedule>=1.2.0  # For scheduled tasks

# Web and API
Flask>=2.3.0
Flask-HTTPAuth>=4.8.0  # For basic authentication
Flask-Caching>=2.1.0
requests>=2.31.0

# Configuration and utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
jsonschema>=4.17.0
reportlab>=3.6.13  # For PDF exports